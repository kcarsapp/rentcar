enum ResetPasswordType {
    email
    phone

    @@schema("enums")
}

enum VerifyStatus {
    pending
    verified
    expired
    failed

    @@schema("enums")
}

enum ContactTypes {
    phone
    email
    whatsapp
    viber

    @@schema("enums")
}

enum ReviewStatus {
    pending
    accepted
    canceled
    flagged
    deleted

    @@schema("enums")
}

enum BookStaus {
    pending
    ongoing
    canceled
    refunded
    rejected
    completed

    @@schema("enums")
}

enum RentalPeriodType {
    hourly
    daily
    weekly
    monthly

    @@schema("enums")
}

enum PromotionPriceType {
    percentage
    fixed

    @@schema("enums")
}

enum PromotionType {
    car
    company
    plan
    rentalPlan

    @@schema("enums")
}

enum Fuel {
    diesel
    gasoline
    electric
    hybird
    lpg
    cng

    @@schema("enums")
}

enum Transmission {
    manual
    automatic
    amt
    cvt
    dct
    sp

    @@schema("enums")
}

enum CompanyStatuses {
    suspended
    loacked
    live

    @@schema("enums")
}

enum SlideType {
    car
    company
    url

    @@schema("enums")
}

enum Currency {
    usd
    iqd

    @@schema("enums")
}
