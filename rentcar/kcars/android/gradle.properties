org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m
# Low-memory machine (page file disabled): cap the Kotlin compile daemon and
# avoid parallel/extra daemons so concurrent JVMs don't exhaust RAM.
kotlin.daemon.jvmargs=-Xmx1024m
kotlin.incremental=false
org.gradle.workers.max=1
org.gradle.parallel=false
org.gradle.daemon=false
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
